	    words = trimline.split()
            numWords = float(len(words))
            chars = float(0)
            for word in words:
                chars += len(word)
            spaces = float(numWords - 1)
            wordSpace = chars	
